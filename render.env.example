# Render 배포를 위한 환경 변수 예시
# 이 파일의 내용을 Render의 환경 변수 설정에 복사하여 사용하세요

# 환경 설정
NODE_ENV=production
PORT=10000

# 데이터베이스 설정
# MongoDB Atlas나 다른 호스팅 서비스의 연결 문자열로 변경해야 합니다
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT 설정
JWT_SECRET=your_secure_jwt_secret
JWT_EXPIRE=30d

# CORS 설정 - 실제 배포된 URL로 변경해야 합니다
CLIENT_URL=https://your-app-url.onrender.com

# 더미 데이터 모드 (MongoDB 연결 실패 시 더미 데이터 사용)
USE_DUMMY_DATA=false
